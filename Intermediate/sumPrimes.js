function sumPrimes(num) {
 	var arr = [];
 	numbers = []
 	for (var i = 1; i < 9999999; i++) {
 		arr.push(i)
 	}
 	for (var x = 1; x < numbers.length; x++) {
 		if(i%numbers==0){
 			var numDivide = 
 		}
 	}
}

sumPrimes(10);
